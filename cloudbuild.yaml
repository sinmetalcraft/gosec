steps:
    - name: 'gcr.io/cloud-builders/docker'
      args: [ 'build', '--build-arg', 'GO_VERSION=1.15', '-t', 'gcr.io/$PROJECT_ID/gosec:$TAG_NAME', '.' ]
images: ['gcr.io/$PROJECT_ID/gosec:$TAG_NAME']
